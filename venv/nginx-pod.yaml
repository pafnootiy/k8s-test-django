apiVersion: v1
kind: Pod
metadata:
  name: nginx-image
  labels:
    name: nginx-image
spec:
  containers:
  - name: nginx-image
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80